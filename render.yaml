services:
  - type: web
    name: project-blog
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:create_app()
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true